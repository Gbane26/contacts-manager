{{ form_start(form) }}

    <div class="row mb-4">
        <div class="col">
        {{ form_widget(form) }}
        </div>
        <div class="col">
        <button data-mdb-ripple-init type="submit" class="btn btn-primary btn-block mb-4">{{ button_label|default('Save') }}</button>
        </div>
    </div>
    
    
{{ form_end(form) }}
